public class Simple_loops {
	
	public static void main(String[] args) {
countSomeNums();
	}
	 /**
	 * This method will check if my int i is less than or equal to 86. If it's true, it'll add 18
	 * to the number. Next, it'll print out on a line the new number for i. As long as i is less
	 * than or equal to 86, it'll repeat the method until i is not less than or equal to 86.
	 */
	public static void countSomeNums() {
	/**
	 * int i is -4
	 */
		int i = -4;
	//This loop will check if -4 is less than or equal to 86. Yad yada, add 18 and repeat.
	
			for(; i<=86; i += 18)
			{
	//Prints out i's new number.

				System.out.println(i);	
			}
	}

}
